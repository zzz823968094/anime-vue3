services:
  anime-vue3:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: anime-vue3-app
    ports:
      - "80:80"
      - "5173:5173"
    restart: unless-stopped
    networks:
      - anime-network

networks:
  anime-network:
    driver: bridge
